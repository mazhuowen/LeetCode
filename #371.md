[toc]

```java
class Solution {
    public int getSum(int a, int b) {
        int base = a ^ b;
        int carry = (a & b) << 1;
        if (carry == 0) return base;
        else return getSum(base, carry);
    }
}
```
